# 仓库协作指令

## 适用范围

除非某个子目录中存在更具体的 `AGENTS.md` 或 `CLAUDE.md`，否则本文件中的规则适用于整个仓库。

后端位于：

```text
backend/
```

## 项目说明

本仓库包含一个面向公司实际办公场景的风电资料管理系统后端。

后端技术栈包括：

- Python
- Django
- Django REST Framework
- MySQL
- 模块化单体架构

工程质量、业务逻辑正确性、权限安全、可测试性和文档可维护性均为强制要求。

## 开发环境

执行后端命令前，先激活项目虚拟环境：

```bash
conda activate doc_system
```

后端相关命令应在以下目录中执行：

```text
backend/
```

## 常用命令

```bash
python manage.py check
pytest
ruff check .
ruff format --check .
mypy apps common
```

只执行与当前任务相关的命令。除非命令已经实际执行并成功完成，否则不得声称该命令已经通过。

## 架构规则

业务代码按 Django App 组织。

各文件职责应保持清晰：

- `models.py`：数据库字段、关系、约束、索引及简单模型行为
- `services.py`：写操作和完整业务流程
- `selectors.py`：查询逻辑和可读数据获取
- `permissions.py`：权限判断和访问规则
- `serializers.py`：请求参数校验和响应数据序列化
- `views.py`：保持轻量，仅负责请求编排

除非现有代码结构明确要求，否则不要把复杂业务逻辑放入 View、Serializer、信号或模型的 `save()` 方法中。

在创建新的抽象、异常类型、响应格式、命名方式或测试模式前，先复用仓库中已有实现。

## 数据库迁移规则

首次业务迁移前，必须完成自定义用户模型并正确配置 `AUTH_USER_MODEL`。

禁止先迁移 Django 默认用户表。

不得编辑、删除或重写可能已经在其他环境中执行过的迁移文件。

所有数据库结构变更都必须通过 Django Migration 完成。

## 安全不变量

以下规则不得违反：

- 不实现公开自助注册；
- 不把文件二进制数据存入 MySQL；
- 不公开真实文件系统路径或可绕过权限的媒体地址；
- 所有业务 API 默认要求身份认证；
- 文件下载必须在后端执行权限判断；
- 不得依靠前端隐藏按钮实现权限控制；
- 不得把真实密码、密钥、令牌或私钥写入源码、文档、日志或 Git 提交；
- 遵循最小权限原则；
- 将权限判断、文件访问和审计逻辑视为安全关键代码。

## 修改纪律

修改代码前必须：

1. 阅读当前目录适用的 `AGENTS.md`、`CLAUDE.md`、README 和计划文档；
2. 检查当前 Git 状态；
3. 阅读与任务相关的现有代码和测试；
4. 保留与当前任务无关的未提交修改；
5. 确认当前里程碑和任务边界。

实施过程中必须遵守：

- 只处理当前明确要求的里程碑；
- 不提前实现后续里程碑；
- 不进行与当前任务无关的大规模重构；
- 没有明确需求时，不引入新的基础设施或依赖；
- 优先采用与现有架构一致的最小正确修改；
- 实现功能的同时新增或更新测试；
- 保证文档与实际代码行为保持一致。

除非用户明确要求，否则不得自动提交 Git、重置分支、丢弃修改或重写历史记录。

## 验证要求

任务完成后，应执行与本次改动相关的检查。

至少应考虑：

```bash
python manage.py check
pytest <相关测试路径>
ruff check .
ruff format --check .
mypy apps common
```

如果涉及迁移，还应执行：

```bash
python manage.py makemigrations --check --dry-run
```

如果因为环境、依赖、MySQL、Docker 或权限限制无法执行某项检查，必须明确说明原因和未验证范围。

## 完成报告

每个任务或里程碑完成后，提供简明报告，至少包含：

1. 新增或修改的文件；
2. 主要实现决策；
3. 实际执行的命令；
4. 实际测试和检查结果；
5. 人工验证步骤；
6. 剩余问题或风险；
7. 推荐的下一步。

报告应保持简洁，不要展开与当前任务无关的长篇说明。

## 工程质量标准

本项目最终将用于公司真实办公环境。

在认为任务完成前，必须确认：

- 业务规则实现正确；
- 权限边界无法被绕过；
- 数据库变更能够安全迁移；
- 错误处理明确；
- 测试覆盖关键成功路径和失败路径；
- 文档反映实际实现；
- 不把未经验证的内容描述为已完成结果。
