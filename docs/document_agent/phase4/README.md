# Document Agent Phase 4

Phase 4 已完成纠正性冻结。开发字段、规则和Prompt时没有读取 B001–B008 的答案正文；
Phase 5真实生成发现的问题已经修复，并要求所有评测输出绑定当前实现指纹后重新生成。

已实现：

- 多来源候选事实合并、冲突保留和成果结果事实拦截；
- 必填事实及条件必填门槛；
- 12类批准风险的确定性画像；
- 16条批准条款的确定性选择和原文校验；
- 版本化事实抽取、章节生成、章节修订和Schema修复Prompt；
- OpenAI兼容LLM Provider、HTTPS和密钥门槛、最多3次请求尝试、最多1次Schema修复；
- Token、费用估算、请求ID和重试次数记录；
- 章节级保存、修订和锁定章节复用；
- 事实精确引用、历史实体、无来源数字、计划语义、报告语义和条款污染校验。

完成门槛：

- 事实引用覆盖率单元验证为100%；
- 规则测试覆盖12类风险和16条条款；
- 条款只能由确定性Repository选择，模型新增、遗漏或改写条款都会校验失败。

冻结版本及SHA-256见 `phase4_manifest.json`。修改字段、风险、条款、RAG、校验器或Prompt
后，原盲测结果必须作废并重新评测。
