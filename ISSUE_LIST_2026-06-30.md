# 问题清单与解决方案

来源：`PROJECT_AUDIT_REPORT_2026-06-30.md`  
目标：完整列出问题，保持简短，便于后续逐项修复。

## P0 必须先处理

### 1. 项目硬删除接口暴露

问题：`ProjectViewSet` 默认暴露 `DELETE /projects/{id}/`，可能让可见项目成员绕过归档、权限和审计直接删除项目。  
解决方案：禁用该 DELETE 接口，或重写为服务层删除；补充不同角色的删除权限测试。

### 2. 目录硬删除接口暴露

问题：`FolderViewSet` 默认暴露 `DELETE /folders/{id}/`，绕过系统根保护、停用规则、目录权限和审计。  
解决方案：禁用目录 DELETE，只保留 `disable`；若需要删除，必须走服务层并补权限测试。

### 3. 文档授权可被改绑

问题：更新文档授权时可修改 `document/user`，服务层只校验旧文档权限，存在越权授权风险。  
解决方案：更新接口禁止修改 `document/user`；如确需迁移授权，必须校验新旧文档权限并写审计。

## P1 高优先级

### 4. 文档授权硬删除绕过撤销

问题：`DocumentGrantViewSet` 默认暴露 DELETE，可能绕过 `revoke` 的撤销时间、撤销人和审计。  
解决方案：禁用授权 DELETE，只允许 `POST /revoke/`；补 DELETE 不可用测试。

### 5. 用户硬删除接口暴露

问题：`UserViewSet` 默认暴露 DELETE，和“停用用户”业务语义冲突，且缺少审计。  
解决方案：禁用用户 DELETE，只允许停用；如必须删除，限制不能删除自己和最后一个管理员。

### 6. 已删除文档仍可能走临时访问

问题：普通下载会拒绝已删除文档，但临时访问创建和消费没有检查文档是否已删除。  
解决方案：创建和消费临时访问时都校验 `document.is_deleted`；回收站详情禁用授权和临时链接。

### 7. 授权管理 e2e 用例失败

问题：`npm run test:e2e` 有 1 个用例失败，文档详情授权管理流程未被端到端覆盖。  
解决方案：修正 e2e 的目录 fixture 或初始选择逻辑，让测试稳定进入文档表格。

## P2 中优先级

### 8. 项目管理按钮未按角色收敛

问题：前端项目管理页面对创建、编辑、成员维护、归档等按钮展示过宽。  
解决方案：按用户角色和项目成员权限隐藏或禁用按钮；后端继续作为最终权限边界。

### 9. 文档操作按钮未按权限收敛

问题：文档表格始终显示下载、修改、移动、新版本、删除、恢复等操作。  
解决方案：后端返回 `allowed_actions`，或前端基于项目成员权限和文档授权计算按钮可见性。

### 10. 前端字段错误解析不匹配后端结构

问题：后端错误结构是 `{ code, message, errors, request_id }`，前端没有正确读取 `errors` 字段。  
解决方案：前端错误归一化优先读取 `data.errors`；增加单元测试。

### 11. 权限回归测试不足

问题：现有测试全通过，但没有覆盖默认硬删除、授权改绑等高风险路径。  
解决方案：补充权限矩阵测试，覆盖不同角色、跨项目、已归档、已删除、停用对象等场景。

## P3 清理与加固

### 12. 本地测试数据和构建产物较多

问题：存在 `backend/data/files/*.bin`、`fronted/dist`、Playwright 报告、缓存等本地产物。  
解决方案：交接或打包前清理忽略产物，仅保留 `.gitkeep`；必要时在 README 写清理命令。

### 13. seed_dev_data 有固定账号和 --force

问题：开发种子数据包含固定密码，并允许 `--force` 在非 DEBUG 环境执行。  
解决方案：生产文档明确禁止执行；可增加二次确认环境变量或移除生产可执行路径。

### 14. 登录缺少节流或锁定

问题：登录失败只做审计，没有节流、锁定或验证码策略。  
解决方案：增加登录失败节流、账号锁定策略，并支持按账号/IP 查询失败记录。

### 15. API 文档直接挂载

问题：`/api/schema/` 和 `/api/docs/` 当前直接暴露。  
解决方案：生产环境限制为系统管理员或内网访问，必要时关闭 Swagger UI。

### 16. 上传校验主要依赖扩展名

问题：上传文件校验主要检查扩展名和大小，扩展名可伪造。  
解决方案：增加 MIME/magic number 校验；后续可接入病毒扫描和预览隔离。

## 建议修复顺序

1. 先处理 1-6 的后端权限和删除/授权风险。
2. 再补 7、11 的测试。
3. 然后处理 8-10 的前端体验和错误展示。
4. 最后处理 12-16 的清理与安全加固。
