## 分阶段开发任务

### Phase 0：业务基线和测试样本

目标：先固定Agent需要解决的真实业务，不写生成代码。

任务：

- 按“合格样本数量最多、并列时近6个月业务量最高”确定一期业务。
- 收集5–20份审核通过的历史Word。
- 选择至少5份作为盲测集，不参与Prompt和规则编写。
- 收集2–3套优先甲方模板。
- 建立项目字段字典，标明类型、是否必填、来源和示例。
- 建立风险标签和条款适用矩阵。
- 标注历史文档章节和可复用状态。
- 确定公司批准的模型和Embedding服务。
- 建立专家评分表。

交付物：

```
样本清单
字段字典
风险标签表
条款适用矩阵
模板清单
盲测清单
模型配置决定
```

完成门槛：

- 技术负责人签字确认字段、条款和盲测集。
- 未满足门槛不得进行真实模型效果验收。

### Phase 1：核心契约和Fake流水线

目标：建立可以完全脱离Django运行的核心骨架。

任务：

- 建立 `engine` 包。
- 实现所有Pydantic DTO。
- 定义Parser、LLM、Embedding、Repository、Renderer和Storage Ports。
- 实现 `FakeLLMProvider`、`FakeEmbeddingProvider`、内存Repository。
- 实现确定性 `GenerationOrchestrator` 状态流转。
- 建立标准错误类型和错误码。
- 使用固定JSON输入跑通Fake全流程。
- 禁止引入Django ORM和RQ。

测试：

- Schema合法和非法输入。
- 状态步骤顺序。
- Tool调用顺序。
- 中间步骤失败后停止。
- 重复执行保持幂等。
- Fake结果可生成完整Trace。

完成门槛：

- 普通Python测试无需Django即可运行。
- 核心包不存在Django、RQ和HTTP请求依赖。
- Fake流程从输入到结果全部通过。

### Phase 2：资料解析和模板引擎

目标：正确读取来源资料并生成格式正确的Word。

任务：

- 使用 `python-docx` 解析标题、段落、列表和表格。
- 使用 `pypdf` 解析文本型PDF。
- 清理目录、页眉页脚和页码噪声。
- 建立标题路径和来源定位。
- 对扫描PDF返回 `SOURCE_UNSUPPORTED`。
- 使用 `docxtpl` 实现封面和字段替换。
- 使用 `python-docx` 插入动态章节、列表和表格。
- 实现模板预检和占位符检查。
- 输出最小测试DOCX。

测试：

- 多级标题。
- 表格表头保留。
- 空段落和特殊符号。
- 中文编号。
- DOCX来源定位。
- PDF页码定位。
- 不支持格式。
- 模板缺少字段。
- 生成Word可正常打开。

完成门槛：

- 一期样本主要章节能够稳定识别。
- 模板测试数据生成无占位符残留。
- 同一文件重复解析结果哈希一致。

### Phase 3：RAG知识库

目标：只检索合格、相关和可追溯的历史章节。

任务：

- 实现章节级Chunker。
- 实现清洗、切分、哈希和去重。
- 实现Embedding Provider。
- 实现内存/JSON知识库供离线测试。
- 实现元数据硬过滤。
- 实现NumPy余弦相似度。
- 实现固定加权评分和Top-3选择。
- 保存检索Trace和引用定位。
- 实现历史项目专有信息黑名单提取。

测试：

- 相同Chunk不重复入库。
- 未审核Chunk永不返回。
- 业务类型不匹配时永不返回。
- 标签和方法匹配影响排序。
- 低相似度时返回空结果。
- Top-3来源可追溯。
- 历史项目实体能够被识别。

完成门槛：

- 人工标注相关章节的 `Hit@3` 达到80%以上。
- 检索结果不存在跨业务误用。
- 所有返回Chunk带完整来源。

### Phase 4：事实、风险、条款和模型生成

目标：形成受控的分章生成能力。

任务：

- 实现候选事实抽取Prompt和Schema。
- 合并多资料事实并检测冲突。
- 实现必填字段确认门槛。
- 实现确定性风险画像。
- 实现条款规则引擎。
- 实现每章Context Builder。
- 实现章节生成Prompt。
- 实现Schema修复。
- 实现模型超时、短重试和费用记录。
- 实现章节级持久化接口。
- 实现历史实体污染检测。

测试：

- 多资料相同事实合并。
- 冲突事实不自动择一。
- 缺失关键事实禁止生成。
- 风险标签对应正确条款。
- 模型不能覆盖确认事实。
- 空RAG结果仍能安全生成或提示不足。
- Schema错误最多修复一次。
- 历史项目名称导致校验失败。
- 已锁定章节不重新生成。

完成门槛：

- 事实引用覆盖率100%。
- 规则测试覆盖全部一期风险标签。
- 不存在模型自行新增安全条款的路径。

### Phase 5：离线端到端评测

目标：在接入平台前证明Agent确实节省工作量。

任务：

- 建立CLI：
  `输入JSON + 来源资料 + 模板 → DOCX + trace.json + validation.json`
- 对至少5个盲测项目生成初稿。
- 由技术负责人逐章评分。
- 记录事实错误、条款错误、RAG命中、人工修改比例和耗时。
- 调整Prompt、字段、规则和Chunk策略。
- 版本化保存评测结果。

完成门槛：

- 零重大虚构事实。
- 零重大安全和技术遗漏。
- 所有项目数字有来源。
- RAG `Hit@3` 不低于80%。
- 至少70%的变化章节只需小幅修改。
- 预计人工编制时间降低至少50%。

未达到门槛不得接入生产平台。

### Phase 6：Django、MySQL和权限接入

目标：把验证通过的核心能力接入现有项目和文档体系。

任务：

- 新建Django模型和迁移。
- 实现ORM版KnowledgeRepository和ClauseRepository。
- 关联Project、DocumentVersion和现有文件存储。
- 实现services、selectors、permissions和API。
- 实现任务状态机和幂等。
- 接入现有审计日志。
- 增加功能开关。
- 禁止在View和Serializer中放复杂生成逻辑。

测试：

- 迁移。
- 项目隔离。
- 各角色权限。
- 归档项目限制。
- 状态非法跳转。
- 幂等创建、生成和导出。
- 关闭功能开关后原系统不变。
- 导出文件经过现有下载权限。

完成门槛：

- 后端集成测试全部通过。
- 所有写操作有审计记录。
- 原有文档API测试无回归。

### Phase 7：RQ和Redis

目标：可靠执行长任务，不阻塞Gunicorn。

任务：

- 增加RQ、django-rq和redis客户端依赖。
- 配置单一队列。
- 使用 `transaction.on_commit()` 入队。
- 实现Worker入口、任务超时、重试和恢复。
- 每章完成后写入MySQL。
- 实现Redis故障和Worker中断处理。
- 实现Worker启动时遗留任务检查。
- 使用Fake Provider完成任务队列集成测试。

测试：

- Redis不可用。
- Worker在生成中退出。
- Job重复执行。
- 模型调用超时。
- 已完成章节恢复。
- 重试次数耗尽。
- 同一任务重复入队。
- Gunicorn请求不执行生成逻辑。

完成门槛：

- Worker故障不影响原系统。
- 任务不重复生成文档版本。
- 长任务不占用Web Worker。
- 失败任务可由用户安全重试。

### Phase 8：前端审核工作流

目标：让业务人员完成输入、确认、审核和导出闭环。

任务：

- 项目详情增加“方案生成”页签。
- 实现模板和来源资料选择。
- 实现分步表单。
- 展示事实来源、置信度、冲突和缺失项。
- 展示生成进度和失败原因。
- 实现章节编辑、锁定和重新生成。
- 展示引用案例和校验问题。
- 实现提交审核、批准和导出。
- 每2秒轮询，结束后停止。

测试：

- 不同角色操作。
- 页面刷新恢复任务。
- 冲突事实确认。
- 生成失败重试。
- 锁定章节。
- 重复导出。
- 归档项目。
- 功能开关关闭。

完成门槛：

- 用户可以从项目资料完成完整闭环。
- 前端不能绕过后端权限和状态机。
- 所有错误均有明确中文提示。

### Phase 9：服务器部署和试点

目标：安全上线，不改变现有系统运行方式。

任务：

- 原生安装Redis，不使用Docker。
- Redis仅监听本机，启用AOF、`noeviction`和内存限制。
- 在现有虚拟环境安装Agent、RQ和文档依赖。
- 增加 `wind-doc-agent-worker.service`。
- Worker使用现有生产环境变量文件和系统用户。
- 配置自动重启、日志和资源限制。
- 先在测试环境执行迁移和完整回归。
- 备份MySQL和文件数据。
- 开启功能开关进行3–5个新项目试点。
- 记录耗时、费用、失败率、修改率和审核意见。

完成门槛：

- Web、MySQL、Redis和RQ Worker健康。
- Worker停止不影响登录、资料管理和下载。
- 试点达到离线验收标准。
- 技术负责人批准后才能增加新业务类型。