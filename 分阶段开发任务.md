## 分阶段开发任务

### 全阶段业务边界

本功能只负责在项目人员、设备或检测作业入场/开工前，自动生成或辅助完成“四措两案”的
撰写、审核和 DOCX 导出。

- 输入：项目基本信息、计划工作范围、计划检测对象/数量/方法、计划工期、拟入场人员及资质、
  工器具与防护用品、甲方模板、技术协议以及现场安全环保要求。
- 输出：用于内部审核、甲方入场报审和现场作业准备的四措两案初稿/审核稿/批准稿。
- 不处理：实测数据、检测结果、缺陷、处理结果和检测结论。
- 不生成：检测报告、试验报告、验收报告、完工报告或竣工资料。
- 不允许根据完工数据反向生成或补写四措两案。

以下所有 Phase 必须遵守该边界；未来新增报告生成能力必须作为独立需求、独立业务类型和独立
评测体系重新立项，不得复用本任务范围直接扩张。

### Phase 0：业务基线和测试样本

目标：先固定“入场前四措两案编制”这一真实业务，不写生成代码。

任务：

- 按“合格样本数量最多、并列时近6个月业务量最高”确定一期业务。
- 收集5–20份审核通过、确实用于入场/开工前报审的历史四措两案Word。
- 选择至少5份作为盲测集，不参与Prompt和规则编写。
- 收集2–3套四措两案优先甲方模板，不得使用检测报告或完工报告模板。
- 建立项目字段字典，标明类型、是否必填、来源和示例。
- 建立风险标签和条款适用矩阵。
- 标注历史文档章节和可复用状态。
- 确定公司批准的模型和Embedding服务。
- 建立专家评分表。

交付物：

```
样本清单
字段字典
风险标签表
条款适用矩阵
模板清单
盲测清单
模型配置决定
```

Phase 1–3 开发门禁：

- 一期业务、开发样本、模板、字段、风险与条款、章节标注、模型配置和评分规则已经批准。
- 技术负责人确认所有开发样本和模板都是入场四措两案，不是完工成果报告。
- Manifest 状态为 `baseline_ready` 或 `approved`。
- 通过后允许依次实施 Phase 1、Phase 2 和 Phase 3；这不表示 Phase 0 已完整完成。

Phase 4 前完整门禁：

- 技术负责人签字确认字段、条款和盲测集。
- 技术负责人确认所有样本和模板都是入场四措两案，不是完工成果报告。
- 至少5份新增盲测样本已由独立保管人隔离，且未参与字段、切分、RAG权重、Prompt和规则编写。
- Manifest 状态为 `approved`，Phase 0 最终完成记录已签字。
- 未满足完整门禁不得进入 Phase 4、编写或调优真实生成Prompt和规则，也不得进行Phase 5
  真实模型效果验收。

### Phase 1：核心契约和Fake流水线

目标：建立可以完全脱离Django运行的核心骨架。

任务：

- 建立 `engine` 包。
- 实现所有Pydantic DTO。
- 在任务契约中固定文档用途为 `entry_four_measures_two_plans`，拒绝报告类用途。
- 定义Parser、LLM、Embedding、Repository、Renderer和Storage Ports。
- 实现 `FakeLLMProvider`、`FakeEmbeddingProvider`、内存Repository。
- 实现确定性 `GenerationOrchestrator` 状态流转。
- 建立标准错误类型和错误码。
- 使用固定JSON输入跑通Fake全流程。
- 禁止引入Django ORM和RQ。

测试：

- Schema合法和非法输入。
- 状态步骤顺序。
- Tool调用顺序。
- 中间步骤失败后停止。
- 重复执行保持幂等。
- Fake结果可生成完整Trace。
- 报告类文档用途、实测结果字段和检测结论字段被契约拒绝。

完成门槛：

- 普通Python测试无需Django即可运行。
- 核心包不存在Django、RQ和HTTP请求依赖。
- Fake流程从输入到结果全部通过。

### Phase 2：入场编制资料解析和四措两案模板引擎

目标：正确读取入场方案编制资料，并生成格式正确的四措两案Word。

任务：

- 支持项目基本信息、计划任务、计划工期、拟入场人员及资质、设备工器具、技术协议和现场
  安全环保要求等来源资料。
- 使用 `python-docx` 解析标题、段落、列表和表格。
- 使用 `pypdf` 解析文本型PDF。
- 清理目录、页眉页脚和页码噪声。
- 建立标题路径和来源定位。
- 对扫描PDF返回 `SOURCE_UNSUPPORTED`。
- 使用 `docxtpl` 实现封面和字段替换。
- 使用 `python-docx` 插入动态章节、列表和表格。
- 实现模板预检和占位符检查。
- 输出最小测试DOCX。
- 不解析或抽取实测结果、缺陷清单和检测结论作为四措两案事实。

测试：

- 多级标题。
- 表格表头保留。
- 空段落和特殊符号。
- 中文编号。
- DOCX来源定位。
- PDF页码定位。
- 不支持格式。
- 模板缺少字段。
- 生成Word可正常打开。
- 使用报告模板或完工结果资料时给出明确的用途不匹配错误。

完成门槛：

- 一期样本主要章节能够稳定识别。
- 模板测试数据生成无占位符残留。
- 同一文件重复解析结果哈希一致。

### Phase 3：RAG知识库

目标：只检索合格、相关和可追溯的历史入场四措两案章节。

任务：

- 实现章节级Chunker。
- 实现清洗、切分、哈希和去重。
- 实现Embedding Provider。
- 实现内存/JSON知识库供离线测试。
- 实现元数据硬过滤。
- 实现NumPy余弦相似度。
- 实现固定加权评分和Top-3选择。
- 保存检索Trace和引用定位。
- 实现历史项目专有信息黑名单提取。
- 从知识库准入层排除检测报告、完工报告、验收报告和竣工资料章节。

测试：

- 相同Chunk不重复入库。
- 未审核Chunk永不返回。
- 业务类型不匹配时永不返回。
- 标签和方法匹配影响排序。
- 低相似度时返回空结果。
- Top-3来源可追溯。
- 历史项目实体能够被识别。
- 报告正文和检测结论永不进入四措两案检索结果。

完成门槛：

- 人工标注相关章节的 `Hit@3` 达到80%以上。
- 检索结果不存在跨业务误用。
- 所有返回Chunk带完整来源。
- Phase 3完成后必须重新执行Phase 0完整门禁；未补齐盲测集不得进入Phase 4。

### Phase 4：事实、风险、条款和模型生成

目标：形成受控的入场四措两案分章生成能力。

任务：

- 实现候选事实抽取Prompt和Schema。
- 合并多资料事实并检测冲突。
- 实现必填字段确认门槛。
- 实现确定性风险画像。
- 实现条款规则引擎。
- 实现每章Context Builder。
- 实现章节生成Prompt。
- 实现Schema修复。
- 实现模型超时、短重试和费用记录。
- 实现章节级持久化接口。
- 实现历史实体污染检测。
- 所有数量和日期都表达为计划工作范围或入场安排，不得表达为已完成结果。
- 在Prompt和校验器中禁止实测数据、缺陷、处理结果、检测结论及报告体例。

测试：

- 多资料相同事实合并。
- 冲突事实不自动择一。
- 缺失关键事实禁止生成。
- 风险标签对应正确条款。
- 模型不能覆盖确认事实。
- 空RAG结果仍能安全生成或提示不足。
- Schema错误最多修复一次。
- 历史项目名称导致校验失败。
- 已锁定章节不重新生成。
- 输入含完工结果时不将其提升为四措两案事实。
- 输出出现“经检测发现”“检测结果表明”等成果报告语义时校验失败。

完成门槛：

- 事实引用覆盖率100%。
- 规则测试覆盖全部一期风险标签。
- 不存在模型自行新增安全条款的路径。

### Phase 5：离线端到端评测

目标：在接入平台前证明Agent确实节省入场四措两案编制工作量。

任务：

- 建立CLI：
  `输入JSON + 入场编制资料 + 四措两案模板 → entry_plan.docx + trace.json + validation.json + review_bundle.json`
- 盲测输入由项目方直接提供到受控本地评测目录，不上传现有平台、不提交Git、不进入RAG。
- 不要求提供包含金额、付款、账户等商业信息的完整合同；可以使用仅保留编制事实的脱敏
  输入资料。
- 对至少3个盲测入场项目生成四措两案初稿。
- 由技术负责人逐章评分。
- 记录事实错误、条款错误、RAG命中、人工修改比例和耗时。
- 调整Prompt、字段、规则和Chunk策略。
- 版本化保存评测结果。

当前进度（2026-07-25）：

- B001、B005、B008已形成Git忽略的私有事实底稿和输入JSON。
- 3个案例均完成8章节Fake Provider冒烟测试及真实Provider生成，确定性校验通过且事实
  引用覆盖率为100%，模型用量记录不存在Fake调用。
- 3个真实输出均包含完整评审包和当前实现指纹，并已完成逐页排版自检；现有3个独立输入
  已满足加急评测的案例数量要求，不再等待补件。
- 4份补充四措两案已作为答案候选隔离，未读取正文，也不得回填为生成输入。
- 金额、账户、电话和身份证号不进入事实底稿；范围不一致或含敏感身份信息的本地副本只在
  私有隔离区保留，不参与评测。
- 无占位符基线已改为从全新DOCX包安全渲染；事实来源版本和段落/页码/表格/引文定位必须
  命中解析结果，防止历史正文残留或虚假引用。
- 真实模型与Embedding连通检查已通过；6份批准开发样本已建立309个真实向量知识块，
  Phase 3真实RAG评测 `Hit@3=0.882`，来源追踪完整且无跨业务污染。
- 评测器已修复为只选择 `ready` 案例，并严格要求每案8章评分、真实模型用量、
  `review_bundle.json`、当前实现指纹和检索证据；未准备的已登记案例不再误报。
- 已生成24行私有专家评审表；技术负责人评分、人工基线耗时和Agent辅助耗时仍留空，等待
  项目方真实验收。

完成门槛：

- 零重大虚构事实。
- 零重大安全和技术遗漏。
- 零实测结果、检测结论和完工报告语义。
- 所有项目数字有来源。
- RAG `Hit@3` 不低于80%。
- 至少70%的变化章节只需小幅修改。
- 预计人工编制时间降低至少50%。

现有3个独立案例允许Phase 6–8在功能默认关闭的条件下加急开发。真实Provider运行已完成；
技术负责人评分和人工耗时验收未完成前，不得打开生产功能开关或投入实际业务。

### Phase 6：Django、MySQL和权限接入

目标：把验证通过的入场四措两案能力接入现有项目和文档体系。

任务：

- 新建Django模型和迁移。
- 实现ORM版KnowledgeRepository和ClauseRepository。
- 关联Project、DocumentVersion和现有文件存储。
- 不为Phase 5盲测资料或合同新增根分类、专用上传入口或生产数据迁移流程。
- 生产任务通过结构化表单补充事实，并只能选择当前用户已有权限的现有项目文档。
- 将正式导出版本归入当前项目的技术方案/入场资料语义，禁止写入报告模板、检测报告或
  竣工资料档案。
- 实现services、selectors、permissions和API。
- 实现任务状态机和幂等。
- 接入现有审计日志。
- 增加功能开关。
- 禁止在View和Serializer中放复杂生成逻辑。

当前进度（2026-07-25）：

- Phase 6开发已完成：新增表和迁移、项目权限、现有文档来源限制、审计、状态机、幂等、
  章节审核和导出API已实现。
- 3个模板、16个条款和309个真实向量知识块已通过SHA-256与Schema校验并幂等导入本地
  开发库；重复导入不产生重复记录。
- 静态架构门禁保证纯引擎不依赖Django、RQ、Redis或平台模块，前端以独立模块挂载。
- 没有为合同或盲测资料增加根分类、上传入口或生产迁移流程。
- 后端需要同时满足 `DOCUMENT_AGENT_ENABLED=true` 和
  `DOCUMENT_AGENT_PHASE5_APPROVED=true` 才开放接口；当前两者默认均为false。
- 当前结论是“开发完成、功能关闭”，不代表Phase 5真实效果验收已经通过。

测试：

- 迁移。
- 项目隔离。
- 各角色权限。
- 归档项目限制。
- 状态非法跳转。
- 幂等创建、生成和导出。
- 关闭功能开关后原系统不变。
- 导出文件经过现有下载权限。
- 集成测试证明导出目录和文档用途不是报告或竣工资料。

完成门槛：

- 后端集成测试全部通过。
- 所有写操作有审计记录。
- 原有文档API测试无回归。

### Phase 7：RQ和Redis

目标：可靠执行长任务，不阻塞Gunicorn。

任务：

- 增加RQ、django-rq和redis客户端依赖。
- 配置单一队列。
- 使用 `transaction.on_commit()` 入队。
- 实现Worker入口、任务超时、重试和恢复。
- 每章完成后写入MySQL。
- 实现Redis故障和Worker中断处理。
- 实现Worker启动时遗留任务检查。
- 使用Fake Provider完成任务队列集成测试。

当前进度（2026-07-25）：

- Phase 7开发已完成：单一RQ队列、事务提交后入队、UUID唯一载荷、30分钟超时、两次重试、
  逐章MySQL保存、重复Job跳过、Redis故障隔离和Worker启动恢复均已实现。
- 事实提取和章节生成都在Worker执行，Gunicorn请求不执行模型调用。
- 生产配置禁止Fake Provider，并要求Redis使用本机地址。
- 本地真实Redis连接与RQ Worker `--burst` 启停已通过；模型和Embedding均有超时、有上限
  瞬时故障重试，Embedding按10条批处理，章节校验失败只允许受控修订一次。

测试：

- Redis不可用。
- Worker在生成中退出。
- Job重复执行。
- 模型调用超时。
- 已完成章节恢复。
- 重试次数耗尽。
- 同一任务重复入队。
- Gunicorn请求不执行生成逻辑。

完成门槛：

- Worker故障不影响原系统。
- 任务不重复生成文档版本。
- 长任务不占用Web Worker。
- 失败任务可由用户安全重试。

### Phase 8：前端审核工作流

目标：让业务人员完成入场四措两案的输入、确认、审核和导出闭环。

任务：

- 项目详情增加“入场资料编制（四措两案）”页签。
- 实现四措两案模板和现有已授权项目文档选择，不新增敏感合同上传或资料分类入口。
- 实现分步表单。
- 展示事实来源、置信度、冲突和缺失项。
- 展示生成进度和失败原因。
- 实现章节编辑、锁定和重新生成。
- 展示引用案例和校验问题。
- 实现提交审核、批准和导出。
- 页面明确提示“本功能不生成检测报告或完工报告”。
- 每2秒轮询，结束后停止。

当前进度（2026-07-25）：

- Phase 8开发已完成：项目详情页签、已有资料选择、自动事实提取与确认、进度轮询、失败重试、
  章节编辑/锁定/重生、来源与校验展示、负责人批准和Word导出均已实现。
- 页面不提供合同或敏感资料上传入口，并固定提示不生成检测报告、实测结论或完工报告。
- 前端 `VITE_DOCUMENT_AGENT_ENABLED` 默认false；Phase 5真实验收前不显示入口。

测试：

- 不同角色操作。
- 页面刷新恢复任务。
- 冲突事实确认。
- 生成失败重试。
- 锁定章节。
- 重复导出。
- 归档项目。
- 功能开关关闭。

完成门槛：

- 用户可以从项目资料完成完整闭环。
- 前端不能绕过后端权限和状态机。
- 所有错误均有明确中文提示。
- 用户无法选择报告模板、检测报告或完工资料作为四措两案生成用途。

### Phase 9：服务器部署和试点

目标：安全上线，不改变现有系统运行方式。

任务：

- 原生安装Redis，不使用Docker。
- Redis仅监听本机，启用AOF、`noeviction`和内存限制。
- 在现有虚拟环境安装Agent、RQ和文档依赖。
- 增加 `wind-doc-agent-worker.service`。
- Worker使用现有生产环境变量文件和系统用户。
- 配置自动重启、日志和资源限制。
- 先在测试环境执行迁移和完整回归。
- 备份MySQL和文件数据。
- 开启功能开关，在3–5个尚未入场/开工的新项目中试点。
- 记录耗时、费用、失败率、修改率和审核意见。

完成门槛：

- Web、MySQL、Redis和RQ Worker健康。
- Worker停止不影响登录、资料管理和下载。
- 试点达到离线验收标准。
- 技术负责人批准后才能增加新业务类型。
