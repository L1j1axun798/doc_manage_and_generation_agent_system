# 里程碑 9 说明

本阶段新增通知中心、审计查询和小规模批量 ZIP 下载。

通知中心按用户隔离，只允许用户查看自己的通知，并支持标记已读和未读。审计查询接口仅系统管理员可访问，支持按用户、动作、资源和结果过滤，以及搜索和排序。

批量下载通过 `POST /api/v1/documents/batch-download/` 下载当前版本文件，单次最多 20 个文件，总未压缩大小限制为 500MB。服务端逐文件执行下载权限判断，ZIP 内文件名冲突会自动追加序号。批量下载不处理已删除文档，不实现后台异步压缩或批量通知。
