# 里程碑 6 说明

本阶段新增 `DocumentGrant` 用户级授权，用于给受限文档补充 `view`、`download`、`update`、`delete`、`restore`、`manage` 六类动作权限。授权支持过期时间和撤销；过期或已撤销的授权不会参与文档可见性和下载判断。授权创建、更新、撤销均写入审计日志。

本阶段不实现临时访问 Token、公开下载链接、批量下载、通知、回收站删除或恢复业务流程。
