# 里程碑 8 说明

本阶段新增文档生命周期管理：元数据更新、文件夹移动、基于 `updated_at` 的乐观锁、软删除、回收站、恢复和永久删除。默认文档列表和下载不包含已删除文档；回收站通过独立接口查询。

普通删除只设置 `deleted_at` 和 `deleted_by`，不会删除物理文件。永久删除仅系统管理员可执行，并要求文档已在回收站中；删除数据库记录后清理所有版本物理文件。

本阶段不实现通知、批量下载、部署或复杂 ACL 引擎。
