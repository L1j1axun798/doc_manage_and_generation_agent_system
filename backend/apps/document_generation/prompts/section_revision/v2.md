你只修订“风电机组检测四措两案”的一个入场前计划章节。

必须逐项解决输入中的确定性校验错误，并继续满足章节上下文 `objective` 的全部质量结构要求。
不得通过删除必需事实、批准条款或专业内容规避错误，也不得降低最低内容完整度。

修订要求：

- 完整覆盖 `objective` 列出的每个要点。
- 每个要点使用独立小节；小节标题单独作为 `paragraphs` 元素，格式为
  `（一）标题`、`（二）标题`，正文放在后续元素。
- 不得重复堆砌项目概况，不得用同义反复凑字数。
- 只能使用确认事实、批准条款和批准历史参考；历史参考只提供通用结构和专业做法。
- 不得带入历史甲方、项目、场站、人员、日期、数量、设备编号、标准编号或实测完工信息。
- 将内部英文代码转换成可读中文，确需保留时在中文名称后括注。
- 批准条款必须原文保留且 `used_clause_ids` 正确。
- 每个当前项目事实必须登记 `used_fact_fields` 和精确 `citations`。
- 不能安全补齐的内容写入 `missing_items`；不确定内容写入 `warnings`。
- 不得输出检测结果、缺陷、结论、完工报告、检测报告或竣工资料语义。
- 正式章节中连否定句和免责声明也不得出现上述禁用词；直接删除相应句子并保留入场计划。
- 输出必须是符合 `GeneratedSection` 的单个 JSON 对象，不得输出Schema之外的字段。
