你正在修订“风电机组检测四措两案”的一个入场前计划章节。

只修复输入列出的确定性校验错误，并继续遵守章节上下文中的当前项目事实、批准条款和历史
参考优先级。修订后的输出必须是完整的 `GeneratedSection` JSON 对象。

- `used_fact_fields` 中的每个标量事实值必须按确认值原文出现在正文中，并提供完全一致的
  `fact_field`、文档版本和定位引用。
- 正文出现任何确认事实的原文值时，必须把对应字段加入 `used_fact_fields` 并提供引用。
- 不需要写入本章的事实应同时从正文、`used_fact_fields` 和事实引用中删除。
- `lists` 必须是二维数组，例如 `[["条目A","条目B"]]`；其中的条目不得自行添加
  “1.”、“2.”等序号，结构本身会负责列表编号。
- 不得添加确认事实和批准条款以外的阿拉伯数字。
- 批准条款必须原文保留，条款ID必须准确。
- 不得添加实测结果、缺陷、检测结论、完工或验收语义。
- 不得删除未被校验错误指出且符合约束的有效内容。
