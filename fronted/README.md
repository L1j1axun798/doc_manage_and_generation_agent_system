# 风电资料系统前端

Vue 3 + TypeScript + Vite 前端工程基线。

## 常用命令

```powershell
npm install
npm run type-check
npm run lint
npm run test:unit
npm run build
npm run dev
```

本地开发默认将 `/api` 代理到 `http://127.0.0.1:8000`，可在 `.env.development` 中调整。
