# 人员位置大屏

新增“人员位置”功能，用于经理通过地图查看员工最近一次上报的位置，辅助人员调度。该功能不是实时追踪。

## 员工端

- 使用浏览器定位获取当前位置；
- 员工登录系统时，如距离上次上报已超过设定时间，则提示重新上报；
- 提供“上报当前位置”按钮；
- 每天建议上报 2～3 次；
- 显示本人最近一次上报时间和上报结果；
- 不要求浏览器关闭后继续后台定位。

## 管理员端

新增“人员位置大屏”，在同一张地图中显示所有员工最近一次有效位置。

地图标记显示：

- 员工姓名；
- 最近上报位置；
- 上报时间；
- 定位精度；
- 位置状态。

位置状态：

- 正常：4 小时内更新；
- 已过期：超过 4 小时未更新；
- 今日未上报；
- 定位失败。

支持点击员工标记查看详细信息，并提供列表展示未上报或位置已过期的人员。

## 后端

新增位置记录，至少保存：

- 用户；
- 经度；
- 纬度；
- 定位精度；
- 地址；
- 上报时间。

提供接口：

```text
POST /api/v1/locations/report/
GET  /api/v1/locations/me/latest/
GET  /api/v1/locations/admin/latest/
```

普通用户只能上报和查看本人位置，只有管理员可以查看全部人员位置。

地图只在管理员打开位置大屏时加载；员工上报只提交经纬度，减少地图 API 调用。所有位置必须显示上报时间，不能标记为实时位置。

## 第一版不实现

- 实时轨迹；
- 路线回放；
- 电子围栏；
- 自动考勤；
- 后台持续定位。
